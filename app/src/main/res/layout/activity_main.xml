<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#202124"
    android:keepScreenOn="true"
    tools:context=".MainActivity">

    <TextClock
        android:id="@+id/clock"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:format24Hour="HH:mm:ss"
        android:gravity="center"
        android:textColor="#89b4f8"
        android:textSize="152sp"
        app:layout_constraintBottom_toTopOf="@id/back"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="12:12:12" />

    <ImageButton
        android:id="@+id/back"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_marginHorizontal="8dp"
        android:background="@drawable/rounded_button"
        android:src="@drawable/ic_round_skip_previous"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/pauseOrContinue"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/clock" />

    <ImageButton
        android:id="@+id/pauseOrContinue"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_marginHorizontal="8dp"
        android:background="@drawable/rounded_button"
        android:src="@drawable/ic_round_pause"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/next"
        app:layout_constraintStart_toEndOf="@id/back"
        app:layout_constraintTop_toBottomOf="@id/clock" />

    <ImageButton
        android:id="@+id/next"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_marginHorizontal="8dp"
        android:background="@drawable/rounded_button"
        android:src="@drawable/ic_round_skip_next"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/pauseOrContinue"
        app:layout_constraintTop_toBottomOf="@id/clock" />

</androidx.constraintlayout.widget.ConstraintLayout>